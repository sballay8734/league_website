// maybe
const seasonalManagers = () => {
  let listOfObjects = [
    {managers2016: ["steve", "cody", "aaron", "donnie"]},
    {managers2017: ["flipp", "shawn", "dom", "aaron"]}
  ]
  return listOfObjects;
}

const seasonsInLeague = (manager) => {
  let totalSeasons = 0;
  // logic to calculate totalSeasons
}

const yearlyWinPercentage = (manager, year) => {
  // logic
}

const allTimeWinPercentage = (manager) => {
  // logic
}

const yearlyAvgPointsPerWeek = (manager) => {
  // logic
}

const allTimeAvgPointsPerWeek = (manager) => {
  // logic
}